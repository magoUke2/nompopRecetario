<div class="container p-4">
    <div class="row">
        <table class="table table-hover table-dark table-striped">
            <thead>
                <tr>
                    <th scope="col">Nombre</th>
                    <th scope="col">Descripcion</th>
                    <th scope="col">Perfil</th>
                    <th scope="col">Editar</th>
                </tr>
            </thead>
            <tbody>
                {{#each links}}
                <tr>
                    <th scope="row">{{recipename}}</th>
                    <td>{{description}}</td>

                    <td><a href="/recetas/profile/{{recipe_id}}" class="btn btn-secondary">Ver</a></td>
                    <td><a href="/recetas/edit/{{recipe_id}}" class="btn btn-secondary">Editar</a></td>
                </tr>
                {{else}}
                <div class="col-md-4 mx-auto">
                    <div class="card card-body text center">
                        <p>No hay recetas creadas aún</p>
                        <a href="/recetas/add">Crea una!</a>
                    </div>
                </div>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>